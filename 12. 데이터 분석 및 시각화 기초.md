# 📘 12일차 TIL: 데이터 분석 및 시각화 기초

## 1. 데이터 분석의 프로세스

데이터 분석은 단순히 코드를 짜는 것이 아니라, **문제 해결을 위한 일련의 과정**입니다.

* **과제 인식**: 무엇을 분석할 것인가? (현시점 과제: 케이블 산업 도메인 학습을 통한 문제 정의)
* **데이터 수집**: 관련된 유효 데이터를 확보하는 단계.
* **데이터 전처리**: 수집된 원본 데이터의 결점(결측치, 이상치)을 수정하여 분석 가능한 상태로 만드는 **가장 중요한 단계**.
* **데이터 분석 및 시각화**: 패턴을 발견하고 통찰(Insight)을 도출하는 단계.

---

## 2. Matplotlib 시각화 기초

가장 기본적인 그래프 도구인 `matplotlib`의 구조와 사용법을 익혔습니다.

### ❶ 그래프 틀 잡기 (Subplot)

`plt.subplot(행, 열, 순서)`를 사용하여 하나의 화면에 여러 그래프를 배치할 수 있습니다.

* **주의**: 인덱스가 0이 아닌 **1**부터 시작한다는 점이 핵심입니다.

### ❷ 산점도 (Scatter Plot) 상세 설명

질문하신 `plt.scatter()`는 두 변수 사이의 **상관관계**를 점으로 표현할 때 사용합니다.

* **사용법**: `plt.scatter(x, y, c='color', s=size, marker='shape')`
* **x, y**: 점이 찍힐 좌표 값들의 리스트.
* **c (color)**: 점의 색상. 단일 색상(예: 'blue') 혹은 값에 따른 색상 리스트를 넣을 수 있습니다.
* **s (size)**: 점의 크기. 숫자 하나를 넣으면 모든 점이 같아지고, 리스트를 넣으면 값에 따라 크기가 달라집니다.
* **marker**: 점의 모양. `'o'`(원), `'^'`(삼각형), `'s'`(사각형) 등이 있습니다.



### ❸ 다양한 그래프 유형

| 유형 | 함수 | 주요 용도 |
| --- | --- | --- |
| **히스토그램** | `plt.hist(bins=N)` | 도수분포표 시각화, 데이터의 분포와 치우침 확인 |
| **막대그래프** | `plt.bar()` | 범주별 수치 비교 (예: 연도별 매출) |
| **상자그래프** | `plt.boxplot()` | 데이터의 사분위수 및 **이상치(Outlier)** 확인 |
| **파이차트** | `plt.pie()` | 전체 대비 비율 확인 (`autopct`로 퍼센트 자동 표시) |

---

## 3. 워드클라우드 (비정형 데이터 분석)

텍스트와 같은 비정형 데이터를 시각화하는 핵심 기술입니다. 단순한 단어의 나열이 아니라 **형태소 분석**을 통한 전처리가 필수입니다.

### 💡 실습 핵심 포인트 (Checklist)

* **형태소 분석(KoNLPy)**: 조사와 어미를 제거하고 의미 있는 '명사'만 추출.
* **불용어 제거**: 분석에 방해되는 무의미한 단어(`것`, `수`, `이`) 필터링.
* **빈도 계산(Counter)**: 단어의 출현 횟수를 딕셔너리 형태로 변환.
* **폰트 설정**: 한글은 깨지기 쉬우므로 `font_path` 설정이 필수 (`malgun` 등).

### 🛠️ 완성된 실습 코드 흐름

```python
# [전제] 파일 읽기 -> 명사 추출 -> 필터링 -> 빈도 계산 -> 시각화
file_path = "한국국제정치학회_총회_회식 기조연설.txt"

# 1. 파일 안전하게 열기 (with문 활용)
with open(file_path, 'r', encoding='utf-8') as f:
    text = f.read()

# 2. 명사 추출 및 전처리 (두 글자 이상만 남기기)
okt = Okt()
unique_nouns = [w for w in okt.nouns(text) if w not in ['것', '수'] and len(w) > 1]

# 3. 빈도 기반 워드클라우드 생성
counts = Counter(unique_nouns)
wc = WordCloud(font_path='malgun', background_color='white').generate_from_frequencies(counts)

# 4. 시각화 출력 (축 숨기기 필수)
plt.imshow(wc, interpolation='bilinear')
plt.axis('off')
plt.show()

```
