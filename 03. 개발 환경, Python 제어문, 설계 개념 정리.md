# 📘 2일차 TIL: 개발 환경, Python 제어문, 설계 개념 정리


## 1. 실행 환경(Runtime Environment)

**실행 환경**이란 작성된 프로그램이 실제로 동작하는 환경을 의미한다.

### Python 실행 환경 구성

```
Application (Python Script)
→ Python Runtime (python.exe / pythonw.exe)
→ Python SDK (표준 라이브러리, 유틸리티)
→ 운영체제(OS)
```

* **Python Runtime**

  * `python.exe` : 콘솔 기반 실행
  * `pythonw.exe` : GUI 기반 실행 (콘솔 없음)
* **Python SDK**

  * 표준 라이브러리 (`math`, `sys`, `os` 등)
  * 개발 및 실행에 필요한 도구 모음

👉 실행 환경은 **“프로그램이 돌아가는 환경”**이다.

---

## 2. 개발 환경(Development Environment)

**개발 환경**은 코드를 작성·수정·디버깅하는 환경이다.

```
IDE(VSCode)
→ Application (Python Script)
→ Python Runtime
→ Python SDK
→ 운영체제
```

* VSCode, PyCharm 같은 **IDE**는 실행 환경을 **포함해서 관리**
* 코드 작성, 자동완성, 디버깅, 실행 버튼 제공

---

## 3. 실행 환경 vs 개발 환경 차이

| 구분  | 실행 환경              | 개발 환경          |
| --- | ------------------ | -------------- |
| 목적  | 프로그램 실행            | 프로그램 개발        |
| 구성  | Runtime + SDK + OS | IDE + 실행 환경    |
| 사용자 | 최종 사용자             | 개발자            |
| 예시  | 서버에서 돌아가는 Python   | VSCode에서 작성/실행 |

👉 **개발 환경 ⊃ 실행 환경**

---

## 4. 아나콘다(Anaconda)와 가상환경

* **아나콘다를 사용하는 주 목적**

  * 패키지 충돌 방지
  * 프로젝트별 독립된 실행 환경 구성

### 가상환경의 필요성

* 프로젝트마다 Python 버전, 라이브러리가 다름
* 하나의 환경에서 관리하면 **의존성 충돌 발생**

### 해결책

* **가상환경**

  * 프로젝트 단위로 Python + 라이브러리 격리
  * Anaconda, venv, poetry 등

---

## 5. 네트워크 모델과 Web 구조

* 여러 네트워크 모델 중 **Web은 서버–클라이언트 구조**를 사용

### 서버–클라이언트 모델

* **Client** : 요청 (브라우저)
* **Server** : 응답 (웹 서버, API 서버)

```
Client → Request → Server
Client ← Response ← Server
```

---

## 6. MVC 디자인 패턴 (중요)

### MVC란?

**역할 분리를 통해 유지보수성과 확장성을 높이는 설계 패턴**

| 구성요소       | 역할            |
| ---------- | ------------- |
| Model      | 데이터 + 비즈니스 로직 |
| View       | 사용자에게 보여지는 화면 |
| Controller | 입력 처리 + 흐름 제어 |

### 동작 흐름

1. 사용자가 View에서 요청
2. Controller가 요청 수신
3. Model에 데이터 처리 요청
4. 결과를 View에 전달

👉 **UI / 로직 / 데이터 분리**

---

## 7. 프레임워크(Framework)

* **프레임워크란**

  * 프로그램의 **뼈대(구조)** 를 제공하는 도구
  * 개발자는 규칙에 맞춰 코드만 작성

### 라이브러리 vs 프레임워크

* 라이브러리: **내가 호출**
* 프레임워크: **프레임워크가 나를 호출 (제어의 역전, IoC)**

### 예시

* Django, Spring, React

---

## 8. 프로그램의 구성 요소

### 프로그램 = 데이터 + 제어 흐름

* **데이터** : 변수, 상수, 객체
* **제어문** : 조건문, 반복문, 함수 호출

### 값 vs 주소

* 기본적으로 변수는 **값을 저장**
* **복합 자료형(list, dict, set 등)** 은

  * 값이 아닌 **객체의 주소(참조)** 를 저장

```python
a = [1, 2, 3]
b = a
```

* `a`와 `b`는 **같은 객체를 가리킴**

---

## 9. Python 제어문 정리

### 연산자 분류

* 산술: `+ - * / % // **`
* 할당: `= += -= *=`
* 비교: `> < >= <= == !=`
* 논리: `and or not`
* 멤버십: `in`, `not in`
* 식별: `is`, `is not` (주소 비교)

---

### if 문 특징

* 조건식은 **True / False**
* 숫자 0 → False, 그 외 → True

```python
if money:
    print("True")
```

---

### 반복문

#### for

```python
for i in arr:
    print(i)
```

#### while

```python
while 조건:
    실행문
```

---

### continue

* 현재 반복만 건너뛰고 다음 반복으로 이동

---

### 리스트 컴프리헨션

```python
[num * 2 for num in a if num % 2 == 0]
```

* 선언적, 간결, 가독성 ↑

---

## 10. 구조 분해 할당 (Unpacking)

### 개념

* 여러 값을 한 번에 변수에 할당

### 예시

```python
a, b = 10, 20
```

```python
x, y, z = [1, 2, 3]
```

```python
first, *rest = [1, 2, 3, 4]
```

---

## 11. 프로젝트 방법론 vs 프로젝트 진행론

| 구분 | 프로젝트 방법론   | 프로젝트 진행론       |
| -- | ---------- | -------------- |
| 초점 | 어떻게 만들 것인가 | 어떻게 관리할 것인가    |
| 예시 | 폭포수, 애자일   | 일정, 인력, 리스크 관리 |

---

## 12. SW 공학적 관점

* **목표**

  * 최소 비용
  * 최대 효과
  * 유지보수성 확보

---

## 13. 함수(Function)의 등장 배경

### 과거 문제

* 코드 중복
* 환경 의존성
* 재사용 어려움

### 해결

* **독립적인 기능 단위 → 함수**
* 객체지향의 출발점

---

## 14. 매개변수(Parameter)

* 함수 정의 시 입력값
* 함수 내부에서 지역 변수로 사용

```python
def add(a, b):
    return a + b
```

* `a`, `b` → 매개변수
* 호출 시 전달 값 → 인자(argument)

---

## 전체 흐름 요약

* 환경 이해 → 설계 개념 → 제어 흐름 → 함수
* **코드는 구조화될수록 비용이 줄어든다**
* 함수는 **재사용성과 유지보수성의 시작점**

---

다음 강의(함수) 예습으로 매우 좋은 흐름이다.
원하면 **함수 → 객체 → 클래스**까지 자연스럽게 이어서 정리해줄 수 있다.
