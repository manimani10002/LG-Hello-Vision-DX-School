## 📅 19일차 TIL: 머신러닝의 기초와 지도학습

### 1. 도구의 이해: Numpy & Pandas

* **Numpy (계산기):** 파이썬의 리스트보다 훨씬 빠른 **ndArray** 구조를 사용합니다. 모든 수치를 벡터와 행렬로 변환하여 컴퓨터가 초고속 연산을 할 수 있게 돕습니다.
* **Pandas (장부):** Numpy를 기반으로 하지만, 사람이 보기 편하도록 **행(Index)과 열(Column)** 의 이름을 붙인 DataFrame 구조를 사용합니다. 데이터 전처리의 핵심 도구입니다.

### 2. 머신러닝의 본질과 목적

* **정의:** 대량의 데이터에서 통계적 규칙(Pattern)을 찾아내어 미지의 데이터를 **예측** 하는 것.
* **데이터 마이닝 vs 머신러닝:** * **데이터 마이닝:** 데이터 속에서 '몰랐던 인사이트'를 발견하는 것이 주된 목적.
* **머신러닝:** 과거의 데이터를 학습하여 미래의 결과($y$)를 정확히 '예측'하는 것이 목적.


* **평가 지표:** 모델의 우수성은 전문가의 주관이 아닌, 오차(Error)의 크기로 결정됩니다.
* $\hat{y}$ (모델의 예측값) $\approx$ $y$ (실제 정답) 이라면 좋은 모델!



### 3. 학습의 분류 (지도 vs 비지도)

* **지도 학습 (Supervised Learning):** 정답($y$)이 있는 데이터를 학습. (예: 타이타닉 생존자 예측)
* **비지도 학습 (Unsupervised Learning):** 정답($y$) 없이 데이터의 유사성만으로 그룹핑. (예: 고객 군집화, 추천 시스템의 초기 필터링)
> *참고: 추천 시스템은 사용자의 과거 '클릭/구매(정답)'를 학습하면 지도학습적 성격도 가집니다.*



### 4. 머신러닝의 분석 흐름 (Pipeline)

1. **목표 설정:** 종속변수($y$, Target) 결정 (예: 생존 여부).
2. **변수 선택:** 독립변수($x$, Feature) 추출 (예: 성별, 나이, 객실 등급).
3. **데이터 전처리 (중요):** 결측치(NaN) 처리, 문자를 숫자로 바꾸는 인코딩, 이상치 제거.
4. **모델 학습:** 알고리즘을 선택해 데이터의 패턴 학습.
5. **평가:** 별도의 테스트 데이터로 정확도 측정.

---

## 💻 지도학습 실습 코드 (붓꽃 데이터 분류)

필기하신 흐름에 맞춰, 데이터를 읽고 분리하여 학습하고 평가하는 전체 과정을 주석과 함께 작성했습니다.

```python
from sklearn.datasets import load_iris
from sklearn.tree import DecisionTreeClassifier
from sklearn.model_selection import train_test_split
from sklearn.metrics import accuracy_score
import pandas as pd

# 1. 데이터 로딩 (Numpy 형태의 Bunch 객체)
iris = load_iris()
iris_data = iris.data    # 독립변수(x) : 피처
iris_label = iris.target # 종속변수(y) : 타겟(정답)

# 2. 분석을 위해 Pandas DataFrame으로 변환
iris_df = pd.DataFrame(data=iris_data, columns=iris.feature_names)
iris_df['label'] = iris.target # 정답 컬럼 추가

# 3. 데이터 전처리 단계 (예시: 결측치 확인)
# 실제 데이터셋(타이타닉 등)에서는 여기서 fillna() 등을 수행함
print(iris_df.isnull().sum()) 

# 4. 데이터 분할 (공부용 문제집/정답지 vs 시험용 문제집/정답지)
# test_size=0.2 : 20%는 시험용으로 남겨둠
X_train, X_test, y_train, y_test = train_test_split(
    iris_data, iris_label, test_size=0.2, random_state=11
)

# 5. 알고리즘 선택 및 모델 객체 생성 (의사결정나무)
dt_clf = DecisionTreeClassifier(random_state=11)

# 6. 모델 학습 (지도학습: 문제와 정답을 함께 던져줌)
dt_clf.fit(X_train, y_train)

# 7. 예측 수행 (시험 문제지만 주고 AI에게 풀어보라고 함)
pred = dt_clf.predict(X_test)

# 8. 평가 (AI가 푼 답(pred)과 실제 정답(y_test)을 비교)
print(f'예측 정확도: {accuracy_score(y_test, pred):.4f}')

```
